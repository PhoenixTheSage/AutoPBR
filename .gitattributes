# Auto detect text files and perform LF normalization
* text=auto
*.pdb filter=lfs diff=lfs merge=lfs -text
*.dll filter=lfs diff=lfs merge=lfs -text

# GitHub Linguist: treat vendored ONNX Runtime headers as non-project code so repo shows as C#
src/AutoPBR.App/onnxruntime_cuda13/ linguist-vendored
