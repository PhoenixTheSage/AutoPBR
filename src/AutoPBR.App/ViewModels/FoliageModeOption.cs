namespace AutoPBR.App.ViewModels;

/// <summary>Display name (localized) and value ("Ignore All", "No Height", "Convert All") for the foliage dropdown.</summary>
public sealed record FoliageModeOption(string DisplayName, string Value);
